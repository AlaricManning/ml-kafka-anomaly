# Replace YOUR_ACR with your Azure Container Registry name, e.g. myregistry.azurecr.io
apiVersion: apps/v1
kind: Deployment
metadata:
  name: producer
  namespace: ml-kafka
spec:
  replicas: 1
  selector:
    matchLabels:
      app: producer
  template:
    metadata:
      labels:
        app: producer
    spec:
      containers:
        - name: producer
          image: alaricmanningmlkafkaacr.azurecr.io/ml-kafka/producer:latest
          env:
            - name: KAFKA_BOOTSTRAP
              value: kafka-cluster-kafka-bootstrap.ml-kafka:9092
            - name: TOPIC
              value: sensor-readings
            - name: INTERVAL_S
              value: "0.5"
            - name: ANOMALY_RATE
              value: "0.05"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
